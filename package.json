{
  "name": "@steinringer/focal-point",
  "version": "1.0.2",
  "description": "",
  "keywords": [
    "focal point",
    "focus point",
    "focus by point",
    "focus image"
  ],
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "preversion": "npm run build && npm run lint && npm run format",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/steinringer/focal-point.git"
  },
  "author": "Peter Zamiska",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/steinringer/focal-point/issues"
  },
  "homepage": "https://github.com/steinringer/focal-point#readme",
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.8.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.9.4"
  }
}
